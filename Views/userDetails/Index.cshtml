@model IEnumerable<MIS4200Team4.Models.userDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nomination.RecognitionId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserProfile.firstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numberOfNominations)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nomination.RecognitionId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserProfile.firstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.numberOfNominations)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.userDetailID }) |
            @Html.ActionLink("Details", "Details", new { id=item.userDetailID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.userDetailID })
        </td>
    </tr>
}

</table>
